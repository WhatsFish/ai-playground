# Server-only secrets. Never commit this file.

# Azure AI (no key — we use the VM's managed identity)
AZURE_AI_ENDPOINT=https://aiplayground56-resource.services.ai.azure.com/openai/v1
AZURE_AI_DEFAULT_MODEL=Llama-3.3-70B-Instruct

# Auth
NEXTAUTH_URL=http://20.89.176.30/chat
NEXTAUTH_SECRET=change-me-with-openssl-rand-hex-32
GITHUB_CLIENT_ID=fill-me-in
GITHUB_CLIENT_SECRET=fill-me-in
ALLOWED_GITHUB_LOGINS=WhatsFish

# Database (reuse traffic-monitor Postgres, separate DB + role)
DATABASE_URL=postgresql://ai_pg:CHANGE_ME@db:5432/ai_playground
